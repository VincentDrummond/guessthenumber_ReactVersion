// the chassis

import React from "react";
import ReactDOM from "react-dom";

import App from "./App";
// handles working with the page styles

import "./index.css";


ReactDOM.render(
    <App />,
    document.getElementById('root')
);
// shove the App into the DOM where the ID "root" is
